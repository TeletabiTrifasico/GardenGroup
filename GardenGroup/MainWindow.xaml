<Window x:Class="GardenGroup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GardenGroup"
        xmlns:views="clr-namespace:GardenGroup.Views"
        xmlns:models="clr-namespace:GardenGroup.ViewModels"
        mc:Ignorable="d"
        Title="Garden Group" Height="720" Width="1280">
    
    <Window.Resources>
        <DataTemplate DataType="{x:Type models:LoginViewModel}">
            <views:Login />
        </DataTemplate>

        <DataTemplate DataType="{x:Type models:DashboardViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Top Navigation Bar -->
                <Border Grid.Row="0" BorderThickness="1" BorderBrush="Black">

                </Border>

                <!-- MVVM - Insert new Views here 
                    Remember to add new buttons in navbar to switch between them!-->
                <Grid Grid.Row="1">
                    <views:Dashboard />
                </Grid>
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ContentControl Content="{Binding CurrentView}" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center"/>
    </Grid>
</Window>
